spring:
  # Database Configuration (Docker 환경 - 로컬에서 실행시)
  datasource:
    url: jdbc:mysql://localhost:3307/timecapsule?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: timecapsule
    password: 880416

  # JPA Configuration (Docker 환경)
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  # Redis Configuration (Docker 환경 - 로컬에서 실행시)
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

# Logging Configuration (Docker 환경)
logging:
  level:
    com.example.timecapsule_backend: DEBUG
    org.springframework.security: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    org.springframework.mail: DEBUG
